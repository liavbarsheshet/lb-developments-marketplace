---
name: claudio-analyze
description: Alias for claudio-analyze-repo. Deeply analyzes the current repository and saves an indexed record to ~/.claudio.
disable-model-invocation: true
---

# claudio-analyze (alias)

This is a shorthand alias for **`/claudio:claudio-analyze-repo`**.

Execute the `claudio-analyze-repo` skill exactly, passing along any arguments
(e.g. `--force`). See `skills/claudio-analyze-repo/SKILL.md` for the full procedure.
