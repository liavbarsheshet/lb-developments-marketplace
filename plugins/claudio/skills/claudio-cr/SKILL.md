---
name: claudio-cr
description: Alias for claudio-code-review. Reviews only the current branch's changes for quality, security, duplication, and claudio-rule compliance.
argument-hint: "[--post]"
disable-model-invocation: true
---

# claudio-cr (alias)

This is a shorthand alias for **`/claudio:claudio-code-review`**.

Execute the `claudio-code-review` skill exactly, passing along any arguments
(e.g. `--post`). See `skills/claudio-code-review/SKILL.md` for the full procedure.
